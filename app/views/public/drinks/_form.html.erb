<%= form_with model: [drink, drink_comment],url: drink_drink_comments_path(drink), method: :post, local: false do |f| %>
<%= f.text_area :comment,class: "w-100  comment-textarea" %>
<%= f.submit "コメントする", class:"btn btn-success" %><br>
<% end %>